<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.finwe.orion360.sdk.basic.examples">

    <!-- For streaming videos from the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- For playing videos from local storage / memory card. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- For copying test content to local storage / memory card. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main menu. -->
        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.MainMenu"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Examples: -->

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalVideoStreamPlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalVideoDownloadPlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalVideoFilePlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalImageDownloadPlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalImageFilePlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.MinimalVRVideoFilePlayer"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.SensorFusion"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.NadirPatch"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.DirectorsCut"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <activity
            android:name="fi.finwe.orion360.sdk.basic.examples.examples.InteractiveHotspots"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen" >
        </activity>

        <!-- For playing videos from an (optional) expansion package (.obb) -->
        <provider
            android:name="fi.finwe.orion360.sdk.basic.examples.ExpansionContentProvider"
            android:authorities="fi.finwe.orion360.sdk.basic.examples.ExpansionContentProvider"
            android:exported="false"
            android:multiprocess="true" >
            <meta-data
                android:name="mainVersion"
                android:value="1000001" />
            <meta-data
                android:name="patchVersion"
                android:value="1000001" />
        </provider>

    </application>

</manifest>
